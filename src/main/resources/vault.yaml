unievent:
  vault:
    enabled: ${VAULT_ENABLED:false}
    uri: ${VAULT_URI:https://localhost:8200}
    token: ${VAULT_TOKEN:}
    secret-path: ${VAULT_SECRET_PATH:secret/data/unievent}
    ca-cert-path: ${VAULT_CACERT_PATH:}
