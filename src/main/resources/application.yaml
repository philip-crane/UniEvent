spring:
  application:
    name: web
  
  datasource:
    url: jdbc:mysql://localhost:3307/unievent?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true #allowPublicKeyRetrieval=true should be removed in production
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
